import 'package:flutter/material.dart';

class PLPRequestJadwalScreen extends StatelessWidget {
  const PLPRequestJadwalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Request Jadwal Praktek'),
    );
  }
}